#include <iostream>
#include <string>

enum Type {
    Bool, 
    Char,
    Int,
    Float,
    String,
};

enum OP {
    PLUS,
    MINUS,
    MULT,
    POW,
    DIV
}

enum Comparison {
    LE,
    LEQ,
    GE,
    GEQ,
    EQ,
    NEQ
}
